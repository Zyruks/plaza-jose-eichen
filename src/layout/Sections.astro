---
const { sectionName, containerClass } = Astro.props
---

<section class={`${sectionName} my-10`}>
  <slot name='outside' />
  <div class={`container mx-auto ${containerClass}`}>
    <slot />
  </div>
</section>
