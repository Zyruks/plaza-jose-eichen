---
import Anchors from "../components/general/Anchors.astro"

type Props = {
  title: string
  linkPrefix?: string
  link: string
  href: string
  complementaryText?: string
}
const { title, link, href, complementaryText, linkPrefix = "" } = Astro.props
let hidden: string
if (!complementaryText) {
  hidden = "hidden"
}
---

<div class='grid w-full content-start md:justify-self-start'>
  <h2 class='text-2xl font-bold'>{title}</h2>

  <Anchors underline link={`${linkPrefix} ${href}`} textSize='text-lg'>
    {link}
  </Anchors>
  <p class={`${hidden} `}>{complementaryText}</p>
</div>
