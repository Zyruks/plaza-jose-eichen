<div class='mx-auto max-w-screen-xl py-10'>
  <div class='grid-container pt-8 mx-4 gap-8 md:gap-8'>
    <slot />
  </div>
</div>

<style>
  .grid-container {
    display: grid;
    --min-item-width: 250px;

    grid-template-columns: repeat(auto-fit, minmax(min(var(--min-item-width), 100%), 1fr));
  }

  @media (min-width: 768px) {
    .grid-container {
      grid-template-rows: repeat(3, 400px);
    }
  }
  @media (min-width: 1000px) {
    .grid-container {
      --min-item-width: 350px;
      grid-template-columns: 1fr 1fr 1fr;
      grid-template-rows: repeat(3, 1fr);
    }
  }
</style>
